#include <stdio.h>

int main()
{
    int num[2];
    /*
      General normal_counting_position - 1
      5 element is at (5-1) in the array
     */
    scanf("%d%d", &num[0], &num[1]);

    printf("num[0]: %d\nnum[1]: %d\n", num[0], num[1]);
    return (0);
}
